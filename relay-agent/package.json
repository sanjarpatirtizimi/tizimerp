{
  "name": "sanjar-patir-relay-agent",
  "version": "1.2.2",
  "private": true,
  "description": "Runs on a device on the same local network as a Hikvision Face ID terminal. Polls the Sanjar Patir backend for newly-added drivers and pushes their photo straight to the terminal over the local network.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test prepare-face-jpeg.test.js hikvision-multipart.test.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "form-data": "^4.0.1",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
